# Spark SQL Visual Notes

Open `spark_sql_performance_visual_notes.md` in VS Code and press `Ctrl+Shift+V`.

Do not separate the Markdown file from the `images` folder. The relative links make the diagrams render inside VS Code Preview, GitHub, Typora, Obsidian, and most Markdown viewers.
